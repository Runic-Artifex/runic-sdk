# RunicAssets.AspNetCore

Maps exact manifest entries to ASP.NET Core GET endpoints while preserving media
type, content length, cache policy, and strong entity tags.

```csharp
app.MapRunicAssets(assets);
```
