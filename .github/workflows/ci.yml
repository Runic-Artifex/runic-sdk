name: SDK CI
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: sdk-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
env:
  DOTNET_CLI_TELEMETRY_OPTOUT: "1"
  DOTNET_NOLOGO: "1"
  CONFIGURATION: Release
jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json
      - uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - uses: actions/cache@v4
        with:
          path: |
            .cache/nuget
            ~/.bun/install/cache
          key: sdk-${{ runner.os }}-${{ hashFiles('bun.lock', '**/Directory.Packages.props', '**/*.csproj', 'global.json') }}
      - run: npm install --global npm@11.16.0 pnpm@11.25.0
      - run: bun run bootstrap
      - name: Install browser for conformance and migration verification
        working-directory: examples/customer-migration/Host/Frontend
        run: |
          bunx --bun playwright-core install --with-deps chromium
          bun -e 'import {chromium} from "playwright-core"; console.log("PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=" + chromium.executablePath())' >> "$GITHUB_ENV"
      - run: bun run verify
      - name: Verify the customer migration through the live bridge
        working-directory: examples/customer-migration/Host/Frontend
        run: bun run test:browser
      - name: Verify both hosts with frontend HMR
        working-directory: examples/customer-migration/Host/Frontend
        run: bun run test:dev
      - name: Retain frontend development logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: host-dev-${{ github.sha }}
          path: artifacts/host-dev
          if-no-files-found: ignore
      - run: bun run verify:templates
      - name: Check generated files and lockfile
        run: git diff --exit-code
      - name: Retain local package candidates
        uses: actions/upload-artifact@v7
        with:
          name: runic-sdk-${{ github.sha }}
          path: artifacts/packages
          if-no-files-found: error
  native:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            rid: linux-x64
            extension: ""
          - os: windows-latest
            rid: win-x64
            extension: .exe
          - os: macos-15
            rid: osx-arm64
            extension: ""
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json
      - name: Build and test managed desktop contracts
        shell: bash
        run: |
          dotnet build RunicSdk.Core.slnx -c Release
          dotnet test tests/dotnet/Runic.Desktop.Tests -c Release --no-build
      - name: Install Linux native WebView prerequisites
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install --yes libwebkit2gtk-4.1-0 xvfb
      - name: Build native window smoke
        run: dotnet build tests/native/Runic.Desktop.WebViewSmoke -c Release
      - name: Exercise native window close interception on Linux
        if: runner.os == 'Linux'
        run: xvfb-run -a dotnet tests/native/Runic.Desktop.WebViewSmoke/bin/Release/net10.0/Runic.Desktop.WebViewSmoke.dll
      - name: Exercise native window close interception on Windows and macOS
        if: runner.os != 'Linux'
        run: dotnet tests/native/Runic.Desktop.WebViewSmoke/bin/Release/net10.0/Runic.Desktop.WebViewSmoke.dll
      - name: Compile the original WPF migration example
        if: runner.os == 'Windows'
        run: dotnet build examples/customer-migration/Wpf/CustomerWpf.csproj -c Release
      - name: Publish generated member bridge with NativeAOT
        shell: bash
        run: |
          dotnet publish tests/native/Runic.Application.Bridge.MemberAotSmoke -c Release -r ${{ matrix.rid }} --self-contained true -p:PublishAot=true -p:IlcTreatWarningsAsErrors=true -o artifacts/native
          ./artifacts/native/Runic.Application.Bridge.MemberAotSmoke${{ matrix.extension }}

  footprint:
    needs: verify
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            rid: linux-x64
          - os: windows-latest
            rid: win-x64
          - os: macos-15
            rid: osx-arm64
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - uses: actions/download-artifact@v8
        with:
          name: runic-sdk-${{ github.sha }}
          path: artifacts/packages
      - name: Build verification tools and shared frontend dependencies
        shell: bash
        run: |
          bun run bootstrap
          dotnet build tools/dotnet-runic-toolkit -c Release
          bun eng/run.mjs build-web
      - name: Install browser and Linux native prerequisites
        shell: bash
        run: |
          bunx --bun playwright-core install --with-deps chromium
          bun -e 'import {chromium} from "playwright-core"; console.log("PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=" + chromium.executablePath())' >> "$GITHUB_ENV"
      - name: Verify minimal Desktop conformance
        run: dotnet test tests/dotnet/Runic.Desktop.Tests -c Release -p:RunicDesktopMinimalHost=true
      - name: Publish, measure and verify all three package consumers
        run: bun run verify:footprint
        env:
          RUNIC_SIZE_RID: ${{ matrix.rid }}
      - name: Retain measurements and actual published distributions
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: host-footprint-${{ matrix.rid }}-${{ github.sha }}
          path: artifacts/host-footprint
          if-no-files-found: warn
